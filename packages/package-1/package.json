{
  "name": "package-1",
  "version": "1.0.3",
  "type": "module",
  "description": "> TODO: description",
  "author": "Antoine Rosenbach <github@rbch.ovh>",
  "homepage": "",
  "license": "ISC",
  "main": "lib/package-1.js",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "npx rollup -c",
    "bump-version:patch": "yarn version --patch --no-git-tag-version && yarn run build",
    "bump-version:minor": "yarn version --minor --no-git-tag-version && yarn run build"
  },
  "devDependencies": {
    "rollup": "^2.70.1"
  }
}
